package com.authguidance.basicmobileapp.plumbing.events

/*
 * An event to start the data reload process
 */
class StartReloadEvent(val causeError: Boolean) {

    /*
     * A dummy method to work around compiler warnings
     */
    fun used() {
    }
}
