package com.authsamples.basicmobileapp.plumbing.events

/*
 * An event raised when the main view needs to present login options or trigger a redirect
 */
class LoginRequiredEvent {

    /*
     * A dummy method to work around compiler warnings
     */
    fun used() {
    }
}
