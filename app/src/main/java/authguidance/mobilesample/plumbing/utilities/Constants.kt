package authguidance.mobilesample.plumbing.utilities

/*
 * Constant values
 */
object Constants {

    // The company id passed when navigating to the transactions fragment
    const val ARG_COMPANY_ID = "COMPANY_ID"

    // The error object passed when navigating to the error fragment
    const val ARG_ERROR_DATA = "ERROR_DATA"

    // The error object passed when navigating to the error fragment
    const val ARG_LOGIN_RESULT = "LOGIN_RESULT"
}