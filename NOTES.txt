VIEW CHANGES
------------
1. Update HeaderButtonsView to use a view model
   Test and fix current problem where all buttons are enabled after logout

2. Can I get the parent from ErrorSummaryView for both current fragment and main activity?
   Can I use a child ErrorViewModel in classes that use ErrorSummaryView?

3. Move API calls to model, with some callbacks for UI updates
   Store error view model in parent view model

4. Error details dialog

5. Activity recreation to test
   Test orientation changes / deep links / logins + logouts

5. Finish device not secured fragment and use a lighter green
   Test topmost and ensure that deep links do not run views they should not

6. Use consistent ids in layout files, with lower case and underscores

7. References to global objects in fragments
   Google to understand issues for React / Swift / Kotlin
   Store optional references and set to null on exit

8. Blank out Okta logoutEndpoint from config and use end session endpoint from metadata
   Reduce logout code
   Ignore logout errors as for other samples
   Add exceptionFromMessage and use it to check responses for null

9. Test back navigation - including with errors - and keep code simple

IOS
---
1. iOS device check to also check biometrics only

2. Deep link when Safari View Controller active for iOS to ignore
   Add isTopMost logic

3. Use view model classes?

4. Use a strings source file in iOS
