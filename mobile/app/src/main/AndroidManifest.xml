<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="authguidance.mobilesample">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
            android:name=".Application"
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme"
            android:networkSecurityConfig="@xml/network_security_config">
        <activity android:name="authguidance.mobilesample.logic.activities.CompaniesActivity"
                  android:label="@string/company_activity_title">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity android:name="authguidance.mobilesample.logic.activities.TransactionsActivity"
                  android:label="@string/transactions_activity_title"/>
        <activity android:name="authguidance.mobilesample.logic.activities.ErrorActivity" />
    </application>

</manifest>